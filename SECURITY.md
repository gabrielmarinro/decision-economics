# Security

This repository is a public portfolio artifact.

Do not commit:

- API keys
- service-account credentials
- OAuth secrets
- production customer data
- private configuration

The public AI implementation follows a Bring Your Own Key pattern. The Gemini API key is supplied by the user and stored locally in the browser.

**Public code. Private credentials.**
